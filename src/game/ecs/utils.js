import * as Phaser from 'phaser';

export const PI = Math.PI;
export const engineCircleType = Phaser.Geom.Circle;
export const engineRectType = Phaser.Geom.Rectangle;
export const engineVector2Type = Phaser.Math.Vector2;
export const engineDistanceClass = Phaser.Math.Distance;
export const engineAngleClass = Phaser.Math.Angle;